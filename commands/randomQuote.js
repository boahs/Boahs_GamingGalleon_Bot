const randomQuotes = () => {
    const quoteArr = [

    ];

    
    
  
    const randomQuotes = Math.floor(Math.random() * testArr.length);
    const randomQuote = quoteArr[randomQuotes];
    return randomQuote;
  };


  
  module.exports = {
    randomQuotes
  };
  